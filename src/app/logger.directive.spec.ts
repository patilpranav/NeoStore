import { LoggerDirective } from './logger.directive';

describe('LoggerDirective', () => {
  it('should create an instance', () => {
    const directive = new LoggerDirective();
    expect(directive).toBeTruthy();
  });
});
